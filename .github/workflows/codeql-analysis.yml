name: "CodeQL"

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
#  schedule:
#    - cron: '53 04 * * 0'

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      security-events: write

    steps:
    - uses: actions/checkout@v2
      with:
        persist-credentials: false
    
    - uses: rcowsill/workflow-scan-action@d19c41d0c01d22a70be13e86337518c043f936c4
      with:
        use-default-queries: false
        extra-queries: ./.github/codeql/wss.qls
        upload: false
